# <center>WSL</center>

> wsl --install <system> 安装指定子系统  
wsl --list --online 查看可安装的linux子系统  
wsl -l -v 查看安装的子系统  
wsl -d <system> 启动指定的子系统  
wsl --shutdown 停止全部子系统  
wsl --terminate <system> 停止指定的子系统  
wsl --set-default <system> 设置默认子系统
